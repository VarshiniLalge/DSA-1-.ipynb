# DSA-1-.ipynb
Problem: Given an array of numbers and a target value, find two numbers whose sum equals the target and return their indexes.  What it solves: It efficiently finds the required pair using a dictionary (seen) in O(n) time, avoiding unnecessary pair-by-pair comparisons.
